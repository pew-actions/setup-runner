{
  "name": "setup-runner",
  "version": "1.0.0",
  "description": "GitHub action to setup PEW runner tooling",
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc && ncc build"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "@vercel/ncc": "^0.36.1",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.1.1"
  },
  "engines": {
    "node": ">= 20"
  }
}
